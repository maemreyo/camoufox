diff --git a/dom/base/RoverfoxStorageManager.cpp b/dom/base/RoverfoxStorageManager.cpp
new file mode 100644
index 0000000000..dcb359d25a
--- /dev/null
+++ b/dom/base/RoverfoxStorageManager.cpp
@@ -0,0 +1,194 @@
+#include "RoverfoxStorageManager.h"
+#include "mozilla/Preferences.h"
+#include "nsXULAppAPI.h"
+#include "mozilla/dom/ContentChild.h"
+#include "nsThreadUtils.h"
+
+namespace mozilla {
+namespace dom {
+
+// Values live on the DEFAULT pref branch. They are session state (per-context
+// values a launch sets up), and default prefs are never written to prefs.js:
+// as user prefs a persistent profile kept them, so a relaunch with a different
+// config still reported the previous session's timezone and navigator values.
+// Reading the default branch alone also ignores user values an older build
+// left in a profile's prefs.js.
+static constexpr auto kKind = PrefValueKind::Default;
+
+/* static */ nsTHashMap<nsCStringHashKey, nsCString>&
+RoverfoxStorageManager::GetLocalCache() {
+  static nsTHashMap<nsCStringHashKey, nsCString> sCache;
+  return sCache;
+}
+
+/* static */ nsTHashSet<nsCString>& RoverfoxStorageManager::GetMissCache() {
+  static nsTHashSet<nsCString> sMisses;
+  return sMisses;
+}
+
+/* static */ mozilla::Mutex&
+RoverfoxStorageManager::GetLocalCacheMutex() {
+  static mozilla::Mutex sMutex("RoverfoxStorageCache");
+  return sMutex;
+}
+
+/* static */ nsCString
+RoverfoxStorageManager::MakePrefName(const nsAString& key) {
+  nsAutoCString prefName("roverfox.s.");
+  AppendUTF16toUTF8(key, prefName);
+  return nsCString(prefName);
+}
+
+static void OnStoragePrefChanged(const char* aPrefName, void*) {
+  RoverfoxStorageManager::ForgetMiss(nsDependentCString(aPrefName));
+}
+
+/* static */ void RoverfoxStorageManager::ForgetMiss(const nsACString& aPrefName) {
+  MutexAutoLock lock(GetLocalCacheMutex());
+  GetMissCache().Remove(aPrefName);
+}
+
+/* static */ void
+RoverfoxStorageManager::SetPrefValue(const nsCString& prefName,
+                                      const nsCString& value) {
+  // Always store locally for immediate reads in this process
+  {
+    MutexAutoLock lock(GetLocalCacheMutex());
+    GetLocalCache().InsertOrUpdate(prefName, nsCString(value));
+    GetMissCache().Remove(prefName);
+  }
+  // Sync to parent for cross-process availability
+  if (XRE_IsParentProcess()) {
+    Preferences::SetCString(prefName.get(), value, kKind);
+  } else {
+    ContentChild* cc = ContentChild::GetSingleton();
+    if (cc) {
+      cc->SendRoverfoxStoragePut(prefName, value);
+    }
+  }
+}
+
+/* static */ void RoverfoxStorageManager::ReadValue(const nsCString& prefName,
+                                                    nsACString& outValue) {
+  // Local cache first (values set in this process)
+  {
+    MutexAutoLock lock(GetLocalCacheMutex());
+    if (auto entry = GetLocalCache().Lookup(prefName)) {
+      outValue = entry.Data();
+    }
+  }
+  // Then Preferences (values synced from other processes)
+  if (outValue.IsEmpty()) {
+    Preferences::GetCString(prefName.get(), outValue, kKind);
+  }
+  if (!outValue.IsEmpty() || XRE_IsParentProcess() || !NS_IsMainThread()) {
+    return;
+  }
+  // Sync IPC read from the parent covers a value another process put that has
+  // not reached this process's prefs yet. Once per key: a sync IPC on every
+  // read of an unset key (every read in a launch without per-context values)
+  // made navigator.hardwareConcurrency, screen.* and (color) queries ~20x
+  // slower than stock, a timing a page can measure. A later put anywhere
+  // clears the miss (pref callback / SetPrefValue).
+  {
+    MutexAutoLock lock(GetLocalCacheMutex());
+    if (GetMissCache().Contains(prefName)) {
+      return;
+    }
+  }
+  static bool sObserving = false;
+  if (!sObserving) {
+    sObserving = true;
+    Preferences::RegisterPrefixCallback(OnStoragePrefChanged, "roverfox.s."_ns);
+  }
+  ContentChild* cc = ContentChild::GetSingleton();
+  if (!cc) {
+    return;
+  }
+  nsCString fromParent;
+  cc->SendRoverfoxStorageGet(prefName, &fromParent);
+  outValue = fromParent;
+  MutexAutoLock lock(GetLocalCacheMutex());
+  if (fromParent.IsEmpty()) {
+    GetMissCache().Insert(prefName);
+  } else {
+    GetLocalCache().InsertOrUpdate(prefName, fromParent);
+  }
+}
+
+/* static */ void
+RoverfoxStorageManager::PutUint(const nsAString& key, uint32_t value) {
+  nsCString prefName = MakePrefName(key);
+  nsAutoCString strValue;
+  strValue.AppendInt(static_cast<int64_t>(value));
+  SetPrefValue(prefName, strValue);
+}
+
+/* static */ bool
+RoverfoxStorageManager::GetUint(const nsAString& key, uint32_t& outValue) {
+  nsAutoCString strValue;
+  ReadValue(MakePrefName(key), strValue);
+  if (!strValue.IsEmpty()) {
+    nsresult parseRv;
+    int64_t parsed = strValue.ToInteger64(&parseRv);
+    if (NS_SUCCEEDED(parseRv) && parsed >= 0) {
+      outValue = static_cast<uint32_t>(parsed);
+      return true;
+    }
+  }
+  return false;
+}
+
+/* static */ void
+RoverfoxStorageManager::PutBool(const nsAString& key, bool value) {
+  nsCString prefName = MakePrefName(key);
+  nsAutoCString strValue(value ? "1" : "0");
+  SetPrefValue(prefName, strValue);
+}
+
+/* static */ bool
+RoverfoxStorageManager::GetBool(const nsAString& key, bool& outValue) {
+  nsAutoCString strValue;
+  ReadValue(MakePrefName(key), strValue);
+  if (!strValue.IsEmpty()) {
+    outValue = strValue.EqualsLiteral("1");
+    return true;
+  }
+  return false;
+}
+
+/* static */ void
+RoverfoxStorageManager::PutString(const nsAString& key,
+                                   const nsAString& value) {
+  nsCString prefName = MakePrefName(key);
+  nsAutoCString utf8Value;
+  CopyUTF16toUTF8(value, utf8Value);
+  SetPrefValue(prefName, utf8Value);
+}
+
+/* static */ bool
+RoverfoxStorageManager::GetString(const nsAString& key, nsAString& outValue) {
+  nsAutoCString utf8Value;
+  ReadValue(MakePrefName(key), utf8Value);
+  if (!utf8Value.IsEmpty()) {
+    CopyUTF8toUTF16(utf8Value, outValue);
+    return true;
+  }
+  return false;
+}
+
+/* static */ void
+RoverfoxStorageManager::Remove(const nsAString& key) {
+  nsCString prefName = MakePrefName(key);
+  {
+    MutexAutoLock lock(GetLocalCacheMutex());
+    GetLocalCache().Remove(prefName);
+  }
+  if (XRE_IsParentProcess()) {
+    // A default pref cannot be deleted; empty reads as unset.
+    Preferences::SetCString(prefName.get(), ""_ns, kKind);
+  }
+}
+
+}  // namespace dom
+}  // namespace mozilla
diff --git a/dom/base/RoverfoxStorageManager.h b/dom/base/RoverfoxStorageManager.h
new file mode 100644
index 0000000000..e6d64378e1
--- /dev/null
+++ b/dom/base/RoverfoxStorageManager.h
@@ -0,0 +1,41 @@
+#ifndef mozilla_dom_RoverfoxStorageManager_h
+#define mozilla_dom_RoverfoxStorageManager_h
+
+#include "nsString.h"
+#include "nsTHashMap.h"
+#include "nsTHashSet.h"
+#include "nsHashKeys.h"
+#include "mozilla/Mutex.h"
+
+namespace mozilla {
+namespace dom {
+
+// Cross-process key-value storage using Firefox Preferences + local cache.
+// Write-through: values stored locally (immediate reads) and sent to parent
+// process via IPDL (cross-process sync via Preferences API).
+// Keys prefixed with "roverfox.s." to avoid conflicts.
+class RoverfoxStorageManager {
+ public:
+  static void PutUint(const nsAString& key, uint32_t value);
+  static bool GetUint(const nsAString& key, uint32_t& outValue);
+  static void PutBool(const nsAString& key, bool value);
+  static bool GetBool(const nsAString& key, bool& outValue);
+  static void PutString(const nsAString& key, const nsAString& value);
+  static bool GetString(const nsAString& key, nsAString& outValue);
+  static void Remove(const nsAString& key);
+  // A put reached this process's prefs: the key is no longer a known miss.
+  static void ForgetMiss(const nsACString& aPrefName);
+
+ private:
+  static nsCString MakePrefName(const nsAString& key);
+  static void SetPrefValue(const nsCString& prefName, const nsCString& value);
+  static void ReadValue(const nsCString& prefName, nsACString& outValue);
+  static nsTHashSet<nsCString>& GetMissCache();
+  static nsTHashMap<nsCStringHashKey, nsCString>& GetLocalCache();
+  static mozilla::Mutex& GetLocalCacheMutex();
+};
+
+}  // namespace dom
+}  // namespace mozilla
+
+#endif  // mozilla_dom_RoverfoxStorageManager_h
diff --git a/dom/base/moz.build b/dom/base/moz.build
index 7e8d942191..e44e7259bf 100644
--- a/dom/base/moz.build
+++ b/dom/base/moz.build
@@ -257,6 +257,7 @@ EXPORTS.mozilla.dom += [
     "RequestCallbackManager.h",
     "ResizeObserver.h",
     "ResponsiveImageSelector.h",
+    "RoverfoxStorageManager.h",
     "SameProcessMessageQueue.h",
     "ScreenLuminance.h",
     "ScreenOrientation.h",
@@ -461,6 +462,7 @@ UNIFIED_SOURCES += [
     "RemoteOuterWindowProxy.cpp",
     "ResizeObserver.cpp",
     "ResponsiveImageSelector.cpp",
+    "RoverfoxStorageManager.cpp",
     "SameProcessMessageQueue.cpp",
     "ScreenLuminance.cpp",
     "ScreenOrientation.cpp",
diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 7cdddd10c1..922ddbc377 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -56,6 +56,7 @@
 #include "mozilla/EventDispatcher.h"
 #include "mozilla/EventListenerManager.h"
 #include "mozilla/EventQueue.h"
+#include "nsIScriptError.h"
 #include "mozilla/ExtensionPolicyService.h"
 #include "mozilla/FloatingPoint.h"
 #include "mozilla/FlushType.h"
@@ -241,6 +242,8 @@
 #include "nsICookieService.h"
 #include "nsID.h"
 #include "nsIDOMStorageManager.h"
+#include "nsDocShell.h"
+#include "mozilla/OriginAttributes.h"
 #include "nsIDOMXULControlElement.h"
 #include "nsIDeviceSensors.h"
 #include "nsIDocShell.h"
diff --git a/dom/canvas/OffscreenCanvas.cpp b/dom/canvas/OffscreenCanvas.cpp
index 7234240711..df84b793dc 100644
--- a/dom/canvas/OffscreenCanvas.cpp
+++ b/dom/canvas/OffscreenCanvas.cpp
@@ -654,6 +654,15 @@ void OffscreenCanvas::ReportBlockedFontFamily(const nsCString& aMsg) const {
   }
 }
 
+mozilla::dom::Document* OffscreenCanvas::GetDocument() const {
+  if (NS_IsMainThread()) {
+    if (nsCOMPtr<nsPIDOMWindowInner> win = do_QueryInterface(GetParentObject())) {
+      return win->GetExtantDoc();
+    }
+  }
+  return nullptr;
+}
+
 bool OffscreenCanvas::IsChrome() const {
   if (NS_IsMainThread()) {
     nsCOMPtr<nsPIDOMWindowInner> win = do_QueryInterface(GetRelevantGlobal());
diff --git a/dom/workers/WorkerPrivate.h b/dom/workers/WorkerPrivate.h
index 7f56e1c86d..80b050c08b 100644
--- a/dom/workers/WorkerPrivate.h
+++ b/dom/workers/WorkerPrivate.h
@@ -1107,8 +1107,6 @@ class WorkerPrivate final
 
   void WorkerScriptLoaded();
 
-  Document* GetDocument() const;
-
   void MemoryPressure();
 
   void UpdateContextOptions(const JS::ContextOptions& aContextOptions);
diff --git a/gfx/thebes/gfxPlatformFontList.cpp b/gfx/thebes/gfxPlatformFontList.cpp
index b3c22e2bb1..6bdcd2a57c 100644
--- a/gfx/thebes/gfxPlatformFontList.cpp
+++ b/gfx/thebes/gfxPlatformFontList.cpp
@@ -448,6 +448,7 @@ class ListFontsVisibilityProvider final : public FontVisibilityProvider {
     return mozilla::Nothing();
   }
   void UserFontSetUpdated(gfxUserFontEntry*) override {}
+  mozilla::dom::Document* GetDocument() const override { return nullptr; }
   using FontVisibilityProvider::ReportBlockedFontFamily;
 
  private:
diff --git a/gfx/thebes/gfxTextRun.cpp b/gfx/thebes/gfxTextRun.cpp
index fc1293be42..cac43a8b1f 100644
--- a/gfx/thebes/gfxTextRun.cpp
+++ b/gfx/thebes/gfxTextRun.cpp
@@ -24,6 +24,7 @@
 #include "mozilla/Likely.h"
 #include "mozilla/MruCache.h"
 #include "mozilla/ServoStyleSet.h"
+#include "mozilla/ServoStyleSetInlines.h"
 #include "mozilla/Sprintf.h"
 #include "mozilla/StaticPresData.h"
 #include "mozilla/UniquePtr.h"
@@ -33,6 +34,9 @@
 #include "nsUnicodeProperties.h"
 #include "SharedFontList-impl.h"
 #include "TextDrawTarget.h"
+#include "mozilla/dom/BrowsingContext.h"
+#include "mozilla/dom/Document.h"
+#include "nsPIDOMWindow.h"
 
 #ifdef XP_WIN
 #  include "gfxWindowsPlatform.h"
@@ -699,7 +703,8 @@ void gfxTextRun::DrawEmphasisMarks(
     gfxContext* aContext, gfxTextRun* aMark, gfxFloat aMarkAdvance,
     gfx::Point aPt, Range aRange, const PropertyProvider* aProvider,
     mozilla::gfx::PaletteCache& aPaletteCache) const {
-  MOZ_ASSERT(aRange.end <= GetLength());
+  MOZ_ASSERT(
+      aRange.end <= GetLength());
 
   EmphasisMarkDrawParams params(aContext, aPaletteCache);
   params.mark = aMark;
@@ -1841,6 +1846,18 @@ gfxFontGroup::gfxFontGroup(FontVisibilityProvider* aFontVisibilityProvider,
       mFontVariantEmoji(aVariantEmoji) {
   // We don't use SetUserFontSet() here, as we want to unconditionally call
   // EnsureFontList() rather than only do UpdateUserFonts() if it changed.
+
+  // Initialize cached user context ID via BrowsingContext (canonical source).
+  mUserContextId = 0;
+  if (mFontVisibilityProvider) {
+    if (auto* doc = mFontVisibilityProvider->GetDocument()) {
+      if (auto* win = doc->GetInnerWindow()) {
+        if (auto* bc = win->GetBrowsingContext()) {
+          mUserContextId = bc->OriginAttributesRef().mUserContextId;
+        }
+      }
+    }
+  }
 }
 
 gfxFontGroup::~gfxFontGroup() {
@@ -2583,8 +2600,9 @@ static Script ResolveScriptForLang(const nsAtom* aLanguage, Script aDefault) {
   static LangScriptCache sCache;
   static RWLock sLock("LangScriptCache lock");
 
-  MOZ_ASSERT(aDefault != Script::INVALID &&
-             aDefault < Script::NUM_SCRIPT_CODES);
+  MOZ_ASSERT(
+      aDefault != Script::INVALID &&
+      aDefault < Script::NUM_SCRIPT_CODES);
 
   {
     // Try to use a cached value without taking an exclusive lock.
@@ -3418,7 +3436,7 @@ already_AddRefed<gfxFont> gfxFontGroup::FindFontForChar(
         font = FindFallbackFaceForChar(ff, aCh, aNextCh, presentation);
         if (font) {
           if (CheckCandidate(font,
-                             {FontMatchType::Kind::kFontGroup, ff.Generic()})) {
+                             {FontMatchType::Kind::kFontGroup, mFonts[i].Generic()})) {
             return font.forget();
           }
         }
diff --git a/gfx/thebes/gfxTextRun.h b/gfx/thebes/gfxTextRun.h
index 85aecf5fdd..79f4df71a4 100644
--- a/gfx/thebes/gfxTextRun.h
+++ b/gfx/thebes/gfxTextRun.h
@@ -1401,6 +1401,8 @@ class gfxFontGroup final : public gfxTextRunFactory {
   uint32_t mFontListGeneration = 0;  // platform font list generation for this
                                      // fontgroup
 
+  uint32_t mUserContextId = 0;  // selects the per-context font list
+
   /**
    * Textrun creation short-cuts for special cases where we don't need to
    * call a font shaper to generate glyphs.
diff --git a/layout/base/nsPresContext.cpp b/layout/base/nsPresContext.cpp
index b2394c6106..da755e553c 100644
--- a/layout/base/nsPresContext.cpp
+++ b/layout/base/nsPresContext.cpp
@@ -426,6 +426,10 @@ bool nsPresContext::IsChrome() const {
   return Document()->IsInChromeDocShell();
 }
 
+mozilla::dom::Document* nsPresContext::GetDocument() const {
+  return Document();
+}
+
 void nsPresContext::GetUserPreferences() {
   if (!GetPresShell()) {
     // No presshell means nothing to do here.  We'll do this when we
diff --git a/toolkit/components/resistfingerprinting/FontVisibilityProvider.h b/toolkit/components/resistfingerprinting/FontVisibilityProvider.h
index 2f71aae0f9..c29d5ad5b5 100644
--- a/toolkit/components/resistfingerprinting/FontVisibilityProvider.h
+++ b/toolkit/components/resistfingerprinting/FontVisibilityProvider.h
@@ -42,6 +42,7 @@ class FontVisibilityProvider {
   virtual nsICookieJarSettings* GetCookieJarSettings() const = 0;
   virtual mozilla::Maybe<FontVisibility> MaybeInheritFontVisibility() const = 0;
   virtual void UserFontSetUpdated(gfxUserFontEntry* aUpdatedFont = nullptr) = 0;
+  virtual mozilla::dom::Document* GetDocument() const = 0;
 
   void ReportBlockedFontFamily(const gfxFontFamily& aFamily) const;
   void ReportBlockedFontFamily(const mozilla::fontlist::Family& aFamily) const;
@@ -58,6 +59,7 @@ class FontVisibilityProvider {
   nsICookieJarSettings* GetCookieJarSettings() const override;                \
   mozilla::Maybe<FontVisibility> MaybeInheritFontVisibility() const override; \
   void UserFontSetUpdated(gfxUserFontEntry* aUpdatedFont = nullptr) override; \
+  mozilla::dom::Document* GetDocument() const override;                       \
   using FontVisibilityProvider::ReportBlockedFontFamily;
 };
 
